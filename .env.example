# Protects POST/GET /api/indexnow (Bearer or ?secret=). Never commit the real value.
# Generate: openssl rand -hex 24
INDEXNOW_SUBMIT_SECRET=

# Optional override for scripts/submit-indexnow.mjs (default: https://markajrenting.ch)
# INDEXNOW_BASE_URL=https://markajrenting.ch
